language: python
python:
    - "2.7"
    - "3.3"
    - "3.4"
    - "3.5"
notifications:
  irc:
    channels:
      - "irc.uvt.nl#gitlama"
    template:
      - "%{repository_slug}#%{build_number} %{message} --> %{build_url}"
    skip_join: true
install:
  - python setup.py install
script:
  - cd test && bash test.sh
